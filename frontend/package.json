{
  "name": "forge-workbench",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser workbench for the Forge IDE framework. Presentation only: it sends commands and queries and renders events.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-transform-types --test test/*.test.mjs"
  },
  "dependencies": {
    "monaco-editor": "0.52.2"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vite": "5.4.11"
  }
}
